import { TuiBaseDialogContext } from '@taiga-ui/cdk/interfaces';
import { TuiIdService } from '@taiga-ui/cdk/services';
import { TuiDialog } from '@taiga-ui/cdk/types';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { BehaviorSubject, Observable } from 'rxjs';
export declare abstract class AbstractTuiDialogService<T extends {}> extends Observable<readonly TuiDialog<T, any>[]> {
    private readonly idService;
    protected abstract readonly component: PolymorpheusContent<TuiDialog<T, any>>;
    protected abstract readonly defaultOptions: T;
    protected readonly dialogs$: BehaviorSubject<readonly TuiDialog<T, any>[]>;
    protected constructor(idService: TuiIdService);
    open<G>(content: PolymorpheusContent<TuiBaseDialogContext<G> & T>, options?: Partial<T>): Observable<G>;
}
