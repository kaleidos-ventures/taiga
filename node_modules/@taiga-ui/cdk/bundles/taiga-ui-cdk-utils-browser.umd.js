(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
    typeof define === 'function' && define.amd ? define('@taiga-ui/cdk/utils/browser', ['exports'], factory) :
    (global = global || self, factory((global['taiga-ui'] = global['taiga-ui'] || {}, global['taiga-ui'].cdk = global['taiga-ui'].cdk || {}, global['taiga-ui'].cdk.utils = global['taiga-ui'].cdk.utils || {}, global['taiga-ui'].cdk.utils.browser = {})));
}(this, (function (exports) { 'use strict';

    function isEdge(userAgent) {
        return userAgent.toLowerCase().includes('edge');
    }

    function isEdgeOlderThan(version, userAgent) {
        var EDGE = 'edge/';
        var currentVersion = parseInt(userAgent.slice(userAgent.toLowerCase().indexOf(EDGE) + EDGE.length), 10);
        return currentVersion < version;
    }

    function isFirefox(userAgent) {
        return userAgent.toLowerCase().includes('firefox');
    }

    function isIE(userAgent) {
        return userAgent.toLowerCase().includes('trident');
    }

    function isSafari(element) {
        var documentRef = element.ownerDocument;
        var windowRef = documentRef && documentRef.defaultView;
        return !!windowRef && 'safari' in windowRef;
    }

    exports.isEdge = isEdge;
    exports.isEdgeOlderThan = isEdgeOlderThan;
    exports.isFirefox = isFirefox;
    exports.isIE = isIE;
    exports.isSafari = isSafari;

    Object.defineProperty(exports, '__esModule', { value: true });

})));
//# sourceMappingURL=taiga-ui-cdk-utils-browser.umd.js.map
