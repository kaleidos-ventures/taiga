import { TuiAriaDialogContext } from '@taiga-ui/cdk/interfaces';
import { Observable } from 'rxjs';
export declare class TuiDialogHostComponent<T extends TuiAriaDialogContext> {
    private readonly dialogs;
    readonly dialogs$: Observable<readonly T[]>;
    constructor(dialogs: ReadonlyArray<Observable<ReadonlyArray<T>>>);
}
