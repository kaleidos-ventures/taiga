import { ComponentFactory, ComponentRef, EmbeddedViewRef, Injector, TemplateRef } from '@angular/core';
import { TuiPortalHostComponent } from './portal-host.component';
/**
 * Service for displaying portals
 */
export declare class TuiPortalService {
    private host?;
    private get safeHost();
    attach(host: TuiPortalHostComponent): void;
    add<C>(componentFactory: ComponentFactory<C>, injector: Injector): ComponentRef<C>;
    remove<C>({ hostView }: ComponentRef<C>): void;
    addTemplate<C>(templateRef: TemplateRef<C>, context?: C): EmbeddedViewRef<C>;
    removeTemplate<C>(viewRef: EmbeddedViewRef<C>): void;
}
