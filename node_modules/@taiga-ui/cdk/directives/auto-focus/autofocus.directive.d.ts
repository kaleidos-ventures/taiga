import { AfterViewInit, ChangeDetectorRef, ElementRef, Renderer2, ViewContainerRef } from '@angular/core';
import { TuiFocusableElementAccessor } from '@taiga-ui/cdk/interfaces';
export declare class TuiAutoFocusDirective implements AfterViewInit {
    private readonly changeDetectorRef;
    private readonly elementRef;
    private readonly tuiFocusableComponent;
    private readonly isIos;
    private readonly renderer;
    private readonly viewContainerRef;
    autoFocus: boolean;
    constructor(changeDetectorRef: ChangeDetectorRef, elementRef: ElementRef<HTMLElement>, tuiFocusableComponent: TuiFocusableElementAccessor | null, isIos: boolean, renderer: Renderer2, viewContainerRef: ViewContainerRef);
    ngAfterViewInit(): void;
    private veryVerySadIosFix;
}
