import { ChangeDetectorRef, ElementRef, EventEmitter } from '@angular/core';
import { TuiDirectiveStylesService } from '@taiga-ui/cdk/services';
import { Observable } from 'rxjs';
export declare class TuiAutofilledDirective {
    readonly tuiAutofilledChange: EventEmitter<boolean>;
    onAnimationStart({ animationName }: AnimationEvent): void;
    constructor(directiveStyles: TuiDirectiveStylesService, { nativeElement }: ElementRef<HTMLInputElement>, changeDetectorRef: ChangeDetectorRef, destroy$: Observable<void>);
}
