import { TuiContextWithImplicit } from '@taiga-ui/cdk/interfaces';
import { TuiLetDirective } from './let.directive';
/**
 * @internal
 */
export declare class TuiLetContext<T> implements TuiContextWithImplicit<T> {
    private readonly internalDirectiveInstance;
    constructor(internalDirectiveInstance: TuiLetDirective<T>);
    get $implicit(): T;
    get tuiLet(): T;
}
