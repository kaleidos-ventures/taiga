import { ElementRef, NgZone } from '@angular/core';
import { Observable } from 'rxjs';
import { TuiParentsScrollService } from './parents-scroll.service';
/**
 * Service that monitors element visibility by subscribing to scrolls
 * and polling with set interval, returns either null or an array
 * of elements that overlap given element edges
 */
export declare class TuiObscuredService extends Observable<null | ReadonlyArray<Element>> {
    private obscured$;
    constructor(parentsScroll$: TuiParentsScrollService, { nativeElement }: ElementRef<Element>, ngZone: NgZone, windowRef: Window, destroy$: Observable<void>);
}
