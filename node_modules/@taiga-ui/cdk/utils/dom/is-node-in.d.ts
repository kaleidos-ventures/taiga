/**
 * Checks if node is inside a specific selector
 *
 * @param node
 * @param selector
 * @return true if node is inside a particular selector
 */
export declare function isNodeIn(node: Node, selector: string): boolean;
