import { ChangeDetectorRef, ElementRef } from '@angular/core';
import { AbstractTuiHint } from '@taiga-ui/core/abstract';
import { TuiHintService } from '@taiga-ui/core/services';
import { Observable } from 'rxjs';
export declare class TuiHintsHostComponent {
    private readonly elementRef;
    private readonly changeDetectorRef;
    /**
     * @awful TODO: remove hints variable
     * and use hints$ after resolving issue with Change Detection
     */
    hints: ReadonlyArray<AbstractTuiHint>;
    constructor(elementRef: ElementRef<HTMLElement>, hints$: TuiHintService, changeDetectorRef: ChangeDetectorRef, destroy$: Observable<void>);
    get clientRect(): ClientRect;
    onHovered(hovered: boolean, directive: AbstractTuiHint): void;
}
