import { ElementRef, NgZone } from '@angular/core';
import { Observable } from 'rxjs';
export declare class TuiLoaderDirective {
    private readonly elementRef;
    private strokeDasharray;
    private strokeDashoffset;
    constructor(animationFrame$: Observable<number>, ngZone: NgZone, destroy$: Observable<void>, elementRef: ElementRef<SVGCircleElement>, userAgent: string);
    private animate;
}
