import { EventEmitter } from '@angular/core';
import { TuiNotification } from '@taiga-ui/core/enums';
import { Observable } from 'rxjs';
export declare const STATUS_ICON: Record<string, string>;
export declare class TuiNotificationComponent {
    readonly closeWord$: Observable<string>;
    hasIcon: boolean;
    status: TuiNotification;
    readonly close: EventEmitter<void>;
    constructor(closeWord$: Observable<string>);
    get icon(): string;
    get hasClose(): boolean;
}
