import { ViewportScroller } from '@angular/common';
import { ElementRef, NgZone, Renderer2 } from '@angular/core';
import { TuiOrientation } from '@taiga-ui/core/enums';
import { Observable } from 'rxjs';
export declare class TuiScrollbarDirective {
    private readonly container;
    private readonly documentRef;
    private readonly windowRef;
    private readonly elementRef;
    private readonly viewportScroller;
    tuiScrollbar: TuiOrientation;
    constructor(ngZone: NgZone, renderer: Renderer2, destroy$: Observable<void>, container: ElementRef<HTMLElement> | null, documentRef: Document, windowRef: Window, elementRef: ElementRef<HTMLElement>, viewportScroller: ViewportScroller);
    private get scrolled();
    private get compensation();
    private get thumb();
    private get view();
    private get computedContainer();
    private getScrolled;
}
