import { ElementRef } from '@angular/core';
export declare function scrollRefFactory({ browserScrollRef, }: TuiScrollbarComponent): ElementRef<HTMLElement>;
export declare class TuiScrollbarComponent {
    /**
     * TODO: remove "any" in new TS version; https://github.com/ng-web-apis/common/pull/6
     */
    private readonly cssRef;
    private readonly elementRef;
    private readonly userAgent;
    hidden: boolean;
    readonly scrollable?: ElementRef<HTMLDivElement>;
    private readonly isLegacy;
    constructor(
    /**
     * TODO: remove "any" in new TS version; https://github.com/ng-web-apis/common/pull/6
     */
    cssRef: any, elementRef: ElementRef<HTMLElement>, userAgent: string);
    get showScrollbars(): boolean;
    get browserScrollRef(): ElementRef<HTMLElement>;
    get showNative(): boolean;
    scrollIntoView(event: CustomEvent<HTMLElement>): void;
}
