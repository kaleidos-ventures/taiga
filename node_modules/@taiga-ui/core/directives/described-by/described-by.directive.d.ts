import { TuiHintService } from '@taiga-ui/core/services';
import { Observable } from 'rxjs';
export declare const DESCRIBED_BY = "_described-by";
/**
 * A directive linking focusable elements and hints for accessibility
 */
export declare class TuiDescribedByDirective {
    tuiDescribedBy: string;
    constructor(hintService: TuiHintService, visibility$: Observable<boolean>);
    get computedDescribedBy(): string | null;
}
