import { AfterViewChecked, ComponentFactoryResolver, ElementRef, Injector, OnDestroy } from '@angular/core';
import { TuiActiveZoneDirective, TuiParentsScrollService, TuiPortalService } from '@taiga-ui/cdk';
import { AbstractTuiDropdown } from '@taiga-ui/core/abstract';
import { TuiDropdown } from '@taiga-ui/core/interfaces';
export declare class TuiDropdownDirective extends AbstractTuiDropdown implements TuiDropdown, AfterViewChecked, OnDestroy {
    readonly refresh$: TuiParentsScrollService;
    set open(value: boolean);
    constructor(componentFactoryResolver: ComponentFactoryResolver, injector: Injector, portalService: TuiPortalService, elementRef: ElementRef<HTMLElement>, activeZone: TuiActiveZoneDirective | null, refresh$: TuiParentsScrollService);
}
