import { ChangeDetectorRef, OnInit } from '@angular/core';
import { SafeHtml } from '@angular/platform-browser';
import { TuiDestroyService } from '@taiga-ui/cdk';
import { TuiSvgService } from '@taiga-ui/core/services';
export declare class TuiSvgDefsHostComponent implements OnInit {
    private readonly svgService;
    private readonly changeDetectorRef;
    private readonly destroy$;
    items: IterableIterator<SafeHtml>;
    isBrowser: boolean;
    constructor(documentRef: Document, svgService: TuiSvgService, changeDetectorRef: ChangeDetectorRef, destroy$: TuiDestroyService, platformId: Object);
    ngOnInit(): void;
}
