import { PipeTransform } from '@angular/core';
export declare class TuiFormatNumberPipe implements PipeTransform {
    /**
     * Formats number adding thousand separators and correct decimal separator
     * padding decimal part with zeroes to given length
     * @param value number
     * @param decimalSeparator
     * @param decimalLimit number of digits of decimal part, null to keep untouched
     */
    transform(value: number, decimalLimit?: number | null, decimalSeparator?: string): string;
}
