import { TuiTextMaskPipeHandler } from '@taiga-ui/core/mask';
import { TuiDecimalSymbol } from '@taiga-ui/core/types';
/**
 * Used to finish a number with zeros to a given precision
 */
export declare function tuiCreateAutoCorrectedNumberPipe(decimalLimit?: number, decimalSymbol?: TuiDecimalSymbol, nativeInput?: HTMLInputElement): TuiTextMaskPipeHandler;
