import { TuiBrightness, TuiSizeL, TuiSizeS } from '@taiga-ui/core';
import { TuiStatus } from '@taiga-ui/kit/enums';
import { Observable } from 'rxjs';
export declare class TuiBadgeComponent {
    readonly mode$: Observable<TuiBrightness | null>;
    value: number | string;
    size: TuiSizeS | TuiSizeL;
    status: TuiStatus;
    hoverable: boolean;
    constructor(mode$: Observable<TuiBrightness | null>);
    get padding(): string;
    get outputValue(): string;
}
