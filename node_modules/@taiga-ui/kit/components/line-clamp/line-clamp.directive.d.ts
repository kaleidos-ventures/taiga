import { ElementRef, NgZone, Renderer2, ViewContainerRef } from '@angular/core';
import { TuiDestroyService } from '@taiga-ui/cdk';
import { Observable } from 'rxjs';
import { TuiLineClampComponent } from './line-clamp.component';
export declare class TuiLineClampDirective {
    constructor(component: TuiLineClampComponent, viewContainerRef: ViewContainerRef, documentRef: Document, renderer: Renderer2, ngZone: NgZone, { nativeElement }: ElementRef<HTMLElement>, destroy$: TuiDestroyService, animationFrame$: Observable<number>, 
    /**
     * TODO: remove "any" in new TS version; https://github.com/ng-web-apis/common/pull/6
     */
    cssRef: any);
}
