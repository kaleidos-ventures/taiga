import { PipeTransform } from '@angular/core';
import { TuiMultiSelectComponent } from './multi-select.component';
export declare class TuiHideSelectedPipe<T> implements PipeTransform {
    private readonly component;
    constructor(component: TuiMultiSelectComponent<T>);
    transform(items: null, component?: unknown): null;
    transform(items: ReadonlyArray<T>, component?: TuiMultiSelectComponent<T>): null;
    private filter;
}
