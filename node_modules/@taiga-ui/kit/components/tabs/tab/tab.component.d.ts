import { ElementRef } from '@angular/core';
import { RouterLinkActive } from '@angular/router';
import { TuiFocusVisibleService } from '@taiga-ui/cdk';
import { TuiBrightness } from '@taiga-ui/core';
import { Observable } from 'rxjs';
export declare class TuiTabComponent {
    private readonly routerLinkActive;
    readonly mode$: Observable<TuiBrightness | null>;
    readonly isIos: boolean;
    readonly isAndroid: boolean;
    focusVisible: boolean;
    constructor(routerLinkActive: RouterLinkActive | null, mode$: Observable<TuiBrightness | null>, mobileAware: boolean, isIos: boolean, isAndroid: boolean, event$: Observable<Event>, { nativeElement }: ElementRef<HTMLElement>, focusVisible$: TuiFocusVisibleService);
    get isActive(): boolean;
}
