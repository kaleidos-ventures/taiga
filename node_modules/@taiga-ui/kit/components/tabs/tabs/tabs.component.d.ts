import { AfterViewChecked, ChangeDetectorRef, ElementRef, EventEmitter, Renderer2 } from '@angular/core';
import { Observable } from 'rxjs';
export declare class TuiTabsComponent implements AfterViewChecked {
    private readonly elementRef;
    private readonly renderer;
    underline: boolean;
    set activeItemIndexSetter(index: number);
    readonly activeItemIndexChange: EventEmitter<number>;
    readonly isIos: boolean;
    readonly isAndroid: boolean;
    private readonly children;
    activeItemIndex: number;
    constructor(elementRef: ElementRef<HTMLElement>, renderer: Renderer2, changeDetectorRef: ChangeDetectorRef, resize$: Observable<void>, isIos: boolean, isAndroid: boolean, mobileAware: boolean);
    get refresh$(): Observable<boolean>;
    get tabs(): ReadonlyArray<HTMLElement>;
    get activeElement(): HTMLElement | null;
    ngAfterViewChecked(): void;
    onActivate(element: HTMLElement): void;
    onKeyDownArrow(current: HTMLElement, step: number): void;
    private scrollTo;
}
