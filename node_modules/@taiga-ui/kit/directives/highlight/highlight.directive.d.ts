import { ElementRef, OnChanges, Renderer2 } from '@angular/core';
import { Observable } from 'rxjs';
export declare class TuiHighlightDirective implements OnChanges {
    private readonly documentRef;
    private readonly elementRef;
    private readonly renderer;
    tuiHighlight: string;
    private readonly highlight;
    private readonly treeWalker;
    constructor(documentRef: Document, elementRef: ElementRef<HTMLElement>, renderer: Renderer2, resize$: Observable<unknown>);
    get match(): boolean;
    ngOnChanges(): void;
    private updateStyles;
    private indexOf;
    private setUpHighlight;
}
