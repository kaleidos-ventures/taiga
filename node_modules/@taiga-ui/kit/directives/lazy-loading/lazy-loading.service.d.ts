import { ChangeDetectorRef } from '@angular/core';
import { Observable } from 'rxjs';
export declare class TuiLazyLoadingService extends Observable<string> {
    private src$;
    constructor(changeDetectorRef: ChangeDetectorRef, destroy$: Observable<void>, intersections$: Observable<IntersectionObserverEntry[]>);
    next(src: string): void;
}
